URGENT: Your file got corrupted during the copy operation. Run these commands immediately:

systemctl stop albpetrol-legal
cd /opt/ceshtje_ligjore/ceshtje_ligjore

# Restore from backup
cp client/src/components/case-table.tsx.backup client/src/components/case-table.tsx

# Clear build cache completely  
rm -rf dist/
rm -rf node_modules/.vite/

# Try building with original file first
npm run build

# If that works, start service
systemctl start albpetrol-legal

# Then we'll apply the fix properly using nano editor instead of cat